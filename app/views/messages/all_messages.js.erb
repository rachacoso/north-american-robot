$('#message-list-<%= @stage %>').hide().html("<%= j (render partial: 'shared/messages/message_list_all', locals: {stage: @stage}) %>").fadeIn(1000);

$(document).foundation();
$(document).foundation('tab', 'reflow');