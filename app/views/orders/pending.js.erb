<% if @order.status == "pending" %>
	window.location.replace("<%= order_url(@order) %>");
<% else %>
	$('#order-item-data').html("<%= j (render partial: 'pending_order') %>");
	$('.overlay#order-item-overlay').fadeIn();
	$('#order-item').fadeIn();
<% end %>

setModalActions();