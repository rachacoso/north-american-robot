// $('#profile-conversation').hide().html("<%= j (render partial: 'conversation', locals:{profile:@profile}) %>").fadeIn(500);

// initConversations();

$(document).foundation();
$(document).foundation('tooltip', 'reflow');