
$('#profile-conversation').hide().html("<%= j(render partial: 'matches/conversation', locals: {profile: @profile}) %>").show();

$(document).foundation();
$(document).foundation('tooltip', 'reflow');
initConversations();