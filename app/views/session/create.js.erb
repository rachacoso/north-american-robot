<% if flash[:notice] %>
	$('#login-error').fadeOut(500).html("<%= flash[:notice] %>").fadeIn(500);
<% else %>
	window.location.replace("<%= @redirect_url %>");
<% end %>


$(document).foundation();
$(document).foundation('equalizer', 'reflow');