$('#collection-<%= @collection.first.class.to_s.downcase %>').hide().html("<%= j (render(partial: "shared/coll_item", collection:  @collection , as: :cb, locals: {identifier: @identifier }) || "<h5>( #{no_item_message} )</h5>".html_safe) %>").fadeIn(100);

$('#a_<%= @new_item_id %>,#a_head_<%= @new_item_id %>').addClass('active');

<%= render partial: 'shared/ajax_message', locals: { updated_item: "Profile" } %>

$(document).foundation();
$(document).foundation('accordion', 'reflow');
initialize();
initializeAutoForm();
