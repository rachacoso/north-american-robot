$('#ajax-success-data').html("<%= updated_item %> Updated <small><%= Time.now.strftime('%H:%M:%S %Z') %></small>");
$('#ajax-message').fadeIn();    

if (typeof ajaxtimer !== 'undefined') {
    clearTimeout(ajaxtimer);
    ajaxtimer = null;
}

var ajaxtimer = window.setTimeout( function() {  
	$('#ajax-message').fadeOut();
}, 1500);