$('#ajax-success-data').html("<%= updated_item %> Updated <small><%= Time.now.strftime('%H:%M:%S %Z') %></small>");
$('#ajax-message').fadeIn();    

if (typeof timer !== 'undefined') {
    clearTimeout(timer);
    timer = null;
}

var timer = window.setTimeout( function() {  
	$('#ajax-message').fadeOut();
}, 1500);