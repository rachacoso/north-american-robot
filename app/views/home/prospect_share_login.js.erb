<% unless params[:email].empty? %>

	<% if @has_a_login %>

		$('#p-share-lg').hide().html("<%= j (render(partial: 'home/prospect_share/ps2')) %>").slideToggle(500);

	<% else %>

		$('#p-share-lg').hide().html("<%= j (render(partial: 'home/prospect_share/ps3')) %>").slideToggle(500);

	<% end %>
	
<% end %>
