<% if @current_user %>
	window.location.href = "<%= view_brand_url(@profile) %>";
<% else %>
	$('#profile-preview').fadeOut(function(){
		$('#profile-preview-data').html("<%= j (render partial: 'restricted_view') %>");
	});
	
	
	$('#profile-preview').fadeIn();
<% end %>