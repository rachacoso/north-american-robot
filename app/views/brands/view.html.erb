<%= render partial: 'shared/v2/header/main' %>

<div class="row">
	<div class="small-12 columns">
		<nav class="breadcrumbs">
			<%= link_to "Beauty", root_url %>
		  <%= link_to "Find", brands_url %>
		  <a class="current" href="#"><%= @profile.company_name %></a>
		</nav>
	</div>
</div>

<% if @allow_redirect %>
	<%= render partial: "/shared/profile/brand_full_profile", locals: {profile: @profile} %>
<% else %>
	<%= render "restricted_view" %>
<% end %>


<%= render partial: 'shared/v2/footer/main' %>