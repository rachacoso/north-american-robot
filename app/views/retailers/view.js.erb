<% if @current_user %>
	window.location.href = "<%= view_retailer_url(@profile) %>";
<% else %>
	$('#profile-preview').fadeOut(function(){
		$('#profile-preview-data').html("<%= j (render partial: 'shared/profile/restricted_view') %>");
	});
	
	
	$('#profile-preview').fadeIn();
<% end %>