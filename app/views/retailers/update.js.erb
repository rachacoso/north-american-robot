<% if @armor_payments %>
	$('#armor-ordering-and-payments').fadeOut(function(){
		$('#armor-ordering-and-payments').html("<%= j (render partial: 'shared/profileform/armor/armor_ordering_and_payments', locals: { company: @retailer }) %>");
	});
	$('#armor-ordering-and-payments').fadeIn();
<% end %>

$('#ajax-success-data').html("Profile Updated <small><%= Time.now.strftime('%H:%M:%S %Z') %></small>");
$('#ajax-message').fadeIn();    

if (typeof timer !== 'undefined') {
    clearTimeout(timer);
    timer = null;
}

var timer = window.setTimeout( function() {  
	$('#ajax-message').fadeOut();
}, 1500);

// $('#ajax-message').fadeIn().delay( 1500 ).fadeOut();