 
Hello!
Landing International has updated an inventory shipment request
 
Next Steps:

1) Review request

PREVIOUS REQUEST
------------------------------------
Requested on <%= adjustment.c_at.in_time_zone("Pacific Time (US & Canada)").strftime("%B %d, %Y at %H:%M:%S %Z") %>

<%= adjustment.product.name %> <%= adjustment.product.item_id.present? ? "(#{adjustment.product.item_id})" : "" %>
<%= previous_data[:units] %> units

<%= previous_data[:comment] %>

<% previous_data[:associated_shipments].each do |a| %>
Included in shipment of <%= a.units %> Units on <%= a.c_at.in_time_zone("Pacific Time (US & Canada)").strftime("%B %d, %Y at %H:%M %Z") %>
<% end %>
<% unless previous_data[:complete] == true %>
Units remaining to be shipped: <%= adjustment.units - adjustment.associated_shipments.sum(:units) %>
<% end %>
------------------------------------
   
UPDATED REQUEST
------------------------------------
Requested on <%= adjustment.c_at.in_time_zone("Pacific Time (US & Canada)").strftime("%B %d, %Y at %H:%M:%S %Z") %>
Updated on <%= adjustment.u_at.in_time_zone("Pacific Time (US & Canada)").strftime("%B %d, %Y at %H:%M:%S %Z") %>

<%= adjustment.product.name %> <%= adjustment.product.item_id.present? ? "(#{adjustment.product.item_id})" : "" %>
<%= adjustment.units %> units

<%= adjustment.comment %>

<% adjustment.associated_shipments.each do |a| %>
Included in shipment of <%= a.units %> Units on <%= a.c_at.in_time_zone("Pacific Time (US & Canada)").strftime("%B %d, %Y at %H:%M %Z") %>
<% end %>
<% unless adjustment.complete? %>
Units remaining to be shipped: <%= adjustment.units - adjustment.associated_shipments.sum(:units) %>
<% end %>
------------------------------------


-->View Request at <%= link %>


2) Ship inventory


3) Submit Shipment Notification with shipment tracking details

-->Submit Shipment Notification at <%= link %>



Check your inventory status at any time on your dashboard at landinginternational.com.

Thanks and have a great day!

 
-Team Landing

 
If you have any questions, please email us at orders@landinginternational.com.