$(document).ready(function() {
 
  $("#owl-front-carousel").owlCarousel({
		// navigation : true, // Show next and prev buttons
		loop:true,
		slideSpeed : 300,
		autoPlay: 5000,
		stopOnHover:true,
		paginationSpeed : 400,
		singleItem:true
  });
 
});